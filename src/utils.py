"""Shared utilities for the driver-drowsiness-detection package.

Small helpers reused across modules (path handling, monotonic clock
wrappers, formatting helpers). Implementations are added as the
adjacent modules need them.
"""
